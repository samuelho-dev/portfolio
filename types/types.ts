export interface Experience {
  employer: string;
  employer_url: string;
  location: string;
  title: string;
  dates: string;
  responsibilities: string[];
}

export interface Form {
  name: string;
  email: string;
  subject: string;
  message: string;
}
